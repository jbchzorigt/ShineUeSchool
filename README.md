# Шинэ Үе сургуулийн вэб сайт

## Технологийн стек (зорилтот)

- **Backend:** Django (REST API, админ дашбоард)
- **Frontend:** Next.js, responsive
- **Анимаци:** GSAP (React дээр `@gsap/react`-ийн `useGSAP` hook ашиглана)

> Одоогийн байдлаар Маам багшийн хуудсыг **статик HTML + GSAP (CDN)** хэлбэрээр
> прототип болгон хийж байна. Дизайн, анимаци батлагдсаны дараа Next.js
> компонент болгон шилжүүлнэ. Скриптүүд (nav.js, hero.js, album.js, schedule.js,
> results.js, main.js, formulas.js) тус бүр нэг компонентод харгалзана.

## Ажиллуулах

### Прототип (статик хуудас)

```bash
node .claude/dev-server.js
```

Дараа нь http://localhost:5173 нээнэ. Зургуудыг `images/` хавтсанд оруулна
(`images/README.txt`-г үзнэ үү).

### Backend (Django)

Шаардлага: Python 3.14+, [uv](https://docs.astral.sh/uv/).

```bash
cd backend
uv sync                                   # сангуудыг суулгана (.venv үүснэ)
uv run manage.py migrate                  # бааз үүсгэнэ (SQLite)
uv run manage.py seed_olympiad            # жишээ хуваарь, үр дүн оруулна
uv run manage.py createsuperuser          # admin хэрэглэгч
uv run manage.py runserver                # http://127.0.0.1:8000
```

Windows терминал дээр кирилл текст хэвлэхэд алдаа гарвал командын өмнө
`PYTHONUTF8=1` (PowerShell: `$env:PYTHONUTF8=1`) тавина.

| Хаяг | Тайлбар |
|------|---------|
| `/admin/` | Хуваарь, үр дүн, албумын зураг оруулах |
| `/api/olympiad/years/` | Хуваарь, үр дүнтэй онуудын жагсаалт |
| `/api/olympiad/schedule/?year=2026` | Тухайн оны шатууд |
| `/api/olympiad/results/?year=2025&grade=9` | Тухайн он, ангийн үр дүн (байртай) |
| `/api/olympiad/album/` | Нийтлэгдсэн албумын зургууд |

Бүтэц: `backend/config/` (тохиргоо), `backend/olympiad/` (model, admin,
serializer, view, seed команд). Дараагийн app-ууд: `news`, `school`, `students`.

## Сайтын бүтэц (шаардлага)

1. **Ү.Маам багшийн хуудас** — animated page ✅ *(одоо хийгдэж байгаа)*
   - Олимпиадын хуваарь: 2024, 2025, 2026
   - Олимпиадын үр дүн: 2024, 2025
2. **Мэдээний булан**
   - Постлогдсон цагаар эрэмбэлэгдэнэ
   - Мэдээ бүр ангилалтай
3. **Танилцуулга** — сургуулийн бүрэлдэхүүн, тэнхим бүрийн багш нарын мэдээлэл
4. **Сургуулийн түүх** — timeline story, эхэлсэн огнооноос өнөөг хүртэлх онцлох үйл явдлууд
5. **Academic calendar** — 2026
6. **Сургуулийн тухай** — Хөтөлбөр, Лабораторууд, Олон улсын хөтөлбөр
7. **Timetable** — Хичээлийн хуваарь
8. **Тэтгэлэг ба төгсөгчид**
9. **Сурагчдын клуб** — СУЗ, Охидын зөвлөл

## Нэмэлт (админ дашбоард, Django)

- Мэдээ оруулах
- Форм үүсгэх
- Судалгаа боловсруулах
- Үр дүн болон тайлан харах
- Олимпиадын дүн оруулах

## Брэнд

Сургуулийн логоны өнгө: хөх `#1E3A8F`, шар `#FFC20E`, цагаан дэвсгэр.
